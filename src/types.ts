export enum Subject {
  Korean = 1,
  Math = 2,
  Social = 3,
  Science = 4,
  Englist = 5,
  General = 6,
}

export enum ModalType {
  Login,
  SignUp,
  Find,
  No,
}

export enum FileType {
  Folder,
  Note,
}

export enum NotesMode {
  All,
  Star,
  Trash,
}
