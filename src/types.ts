export enum Subject {
  Korean,
  Math,
  Social,
  Science,
  General,
}

export enum ModalType {
  Login,
  SignUp,
  Find,
  No,
}

export enum FileType {
  Folder,
  Note,
}

export enum NotesMode {
  All,
  Star,
  Trash,
}

export enum ContextMode {
  No,
  Note,
  Folder,
}
